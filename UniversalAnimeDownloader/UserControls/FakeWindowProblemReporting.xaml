<Window x:Class="UniversalAnimeDownloader.UserControls.FakeWindowProblemReporting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversalAnimeDownloader.UserControls"
        Icon="pack://application:,,,/Resources/WinDefaultIcon.png"
        mc:Ignorable="d"
        Title="Program" Height="180" Width="530" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Topmost="True">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Foreground="#005ACE" Text="Program is not responding" FontSize="18"/>
            <TextBlock Text="Hang on while Windows reports the problem to Microsoft..." FontSize="18" Foreground="Black" Margin="0,10,0,0"/>
            <ProgressBar IsIndeterminate="True" Style="{x:Null}" Height="15" Margin="0,10,0,0"/>
        </StackPanel>
        <StatusBar VerticalAlignment="Bottom" Height="50" FlowDirection="RightToLeft">
            <Button Style="{x:Null}" HorizontalAlignment="Right" Margin="10,0,10,0" Click="Button_Click">
                <TextBlock Text="Cancel" Style="{x:Null}" FontSize="24"/>
            </Button>
        </StatusBar>
        
        
    </Grid>
</Window>
